<% content_for :title, "Create New Post" %>

<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><%= link_to "Community", community_path %></li>
          <li class="breadcrumb-item active" aria-current="page">Create New Post</li>
        </ol>
      </nav>

      <div class="card">
        <div class="card-header">
          <h2 class="card-title mb-0">Create New Community Post</h2>
        </div>
        <div class="card-body">
          <%= form_with(model: @post, local: true, class: "row g-3") do |form| %>
            <% if @post.errors.any? %>
              <div class="col-12">
                <div class="alert alert-danger" role="alert">
                  <h5>Please fix the following errors:</h5>
                  <ul class="mb-0">
                    <% @post.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              </div>
            <% end %>

            <div class="col-12">
              <%= form.label :title, class: "form-label" %>
              <%= form.text_field :title, class: "form-control", placeholder: "Enter post title" %>
            </div>

            <div class="col-12">
              <%= form.label :author, class: "form-label" %>
              <%= form.text_field :author, class: "form-control", placeholder: "Your name" %>
            </div>

            <div class="col-12">
              <%= form.label :content, class: "form-label" %>
              <%= form.text_area :content, class: "form-control", rows: 8, placeholder: "Write your post content here..." %>
            </div>

            <div class="col-12">
              <div class="form-check">
                <%= form.check_box :published, class: "form-check-input" %>
                <%= form.label :published, "Publish immediately", class: "form-check-label" %>
              </div>
            </div>

            <div class="col-12">
              <%= form.submit "Create Post", class: "btn btn-danger me-2" %>
              <%= link_to "Cancel", community_path, class: "btn btn-secondary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
